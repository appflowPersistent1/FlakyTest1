# Reproduction: Issue with SvelteKit and Cypress

Install and start dev server:

```
npm install
npm run dev
```

Open Cypress:

```
npx cypress open
```

There is a single file and test. Re-run and the test will fail eventually.
